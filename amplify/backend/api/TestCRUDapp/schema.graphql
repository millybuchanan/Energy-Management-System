type SurveyData @model @auth(rules: [{allow: public}]) {
  id: ID!
  light: Int
  temperature: Int
  pwm: Int
}

type TestModel @model @auth(rules: [{allow: public}]) {
  id: ID!
  username: String!
  password: String!
}
 