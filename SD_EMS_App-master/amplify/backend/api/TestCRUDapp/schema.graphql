type SurveyData @model @auth(rules: [{allow: public}]) {
  id: ID!
  idealBrightness: Float
  idealTemp: Float
  age: Int
  zipcode: Int
  username: String
}

type TestModel @model @auth(rules: [{allow: public}]) {
  id: ID!
  username: String!
  password: String!
  newUser: Boolean!
}
 